---
/**
 * The spaces screen at the size the app needs to draw all three panels.
 *
 * Drawn at the app's own size: the page that places it does the scaling.
 */
import AppShell from '../AppShell.astro';
import SpacesScreen from '../SpacesScreen.astro';
---

<AppShell nav="spaces">
	<SpacesScreen rulesWidth={340} />
</AppShell>
