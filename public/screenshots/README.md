# Docs screenshots

Real app captures referenced by the docs pages. Drop PNGs here with the exact
filenames the pages expect (see each page's `/screenshots/<name>.png` references).
Keep captures at a consistent scale; crop to the relevant region, not the whole desktop.
